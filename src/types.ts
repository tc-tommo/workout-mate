type Exercise = {
    exercise: string;
    sets: number;
    reps: number;
    rest: string;
    equipment: string | null;
    video: string | null;
    notes: string | null;
};

export type { Exercise };
